export default function RonjaPage() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', backgroundColor: 'red' }}>
      Hola Ronja buscador
      <input type='select' placeholder='pon lo que quieras'></input>
      <button>Buscar</button>
      <img href="sad" />
    </div>
  );
}
